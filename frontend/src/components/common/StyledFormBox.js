import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const StyledFormBox = styled(
  Box,
  {}
)({
  display: "flex",
  alignItems: "center",
  alignContent: "stretch",
  flexDirection: "column",
});
