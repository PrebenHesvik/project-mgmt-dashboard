import { styled } from "@mui/material/styles";
import { Typography } from "@mui/material";

export const StyledFormTitle = styled(
  Typography,
  {}
)({
  color: "rgb(59, 58, 58)",
  marginBottom: 30,
  textAlign: "center",
});
